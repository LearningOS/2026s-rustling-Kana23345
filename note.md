
---

# Rustlings 学习笔记：二叉搜索树 (BST)

## 2026-03-23 专题：递归与所有权

### 1. 递归结构的“套娃”限制
* **核心痛点**：在 Rust 中，结构体的大小必须在编译期确定。如果 `TreeNode` 直接包含 `TreeNode`，大小会变成无穷大。
* **写法模板**：使用 `Option<Box<TreeNode<T>>>`。
    * `Box`：将节点扔进**堆内存**，栈上只留一个 8 字节的指针（钥匙）。
    * `Option`：优雅地处理“没有孩子”（`None`）的情况，代替 C++ 的 `nullptr`。

### 2. BST 逻辑的“左右互搏”
* **口诀**：**“左小右大”**。
* **推导过程**：
    * `Ordering::Greater`：代表 `当前节点 > 目标值` $\rightarrow$ 目标值更小 $\rightarrow$ **左转**。
    * `Ordering::Less`：代表 `当前节点 < 目标值` $\rightarrow$ 目标值更大 $\rightarrow$ **右转**。
* **避坑**：逻辑写反是新手最常见的错误，写代码前先在大脑里模拟一下数字 3 插入数字 5 的场景。

### 3. 所有权的“借用”艺术 (`&` vs `&mut`)
* **只读查找 (`search`)**：使用 `match &self.root`。
    * **避坑**：如果不加 `&`，Rust 会试图把根节点从树里“抽出来”挪走（Move），导致整棵树报废。
* **可变修改 (`insert`)**：使用 `match &mut self.left`。
    * **避坑**：只有拿到 `&mut`（可变引用），你才有权限给这个节点“生个孩子”（挂载新节点）。

### 4. 架构分工模型：外壳与核心
* **外壳层 (`BinarySearchTree`)**：
    * 职责：大堂经理。只负责看一眼根节点（`root`）是不是空的。如果是空的，亲手塞入第一个节点；如果不是，把活儿派给 `root`。
* **核心层 (`TreeNode`)**：
    * 职责：基层员工。负责具体的比较、递归往下传、或者在发现空位（`None`）时原地占坑。

### 5. 语法避坑指南
* **拒绝 `unwrap()`**：这是 Rust 的自杀开关。在递归树中，永远优先使用 `match` 或 `if let`。
* **自动解引用 (Deref)**：当你拿到 `left`（类型是 `Box<TreeNode>`）时，直接调用 `left.insert()` 即可。Rust 会自动帮你开箱，不需要像 C++ 那样写 `(*left).insert()`。
* **impl 块的作用域**：带 `self` 的方法必须写在 `impl` 内部。如果写在外面，它只是一个普通的孤立函数，无法通过 `.` 来调用。



### 6. 今日方法论
1. **先看引脚，再接电路**：写递归前先对齐 `&`（只读）还是 `&mut`（修改），类型不对，编译器绝不放行。
2. **绘图辅助**：对于 `Greater/Less` 这种容易混淆的方向，在草稿纸上画个简单的 `5 -> 3` 和 `5 -> 7` 路径，比盯着代码干想强。
3. **小步快跑**：先修好 `TreeNode` 的核心逻辑，再修 `BST` 的外壳入口，最后跑测试。

---

**这份 1.0 版的 BST 笔记记好后，想不想尝试挑战一下 BST 的“中序遍历”？它可以帮你把树里的数字按从小到大的顺序全部打印出来，是检验 BST 是否成功的终极手段。**